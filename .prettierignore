node_modules/
dist/
build/
.next/
coverage/
*.tsbuildinfo
next-env.d.ts
**/*.d.ts

# Generated benchmark reports — written verbatim by `JSON.stringify(report, null, 2)`
# in benchmarks/*/runner.ts / reporter.ts and regenerated by `npm run benchmark:*`;
# formatting them only gets undone on the next run.
benchmarks/reports/

# Git-ignored tooling scratch (SDD ledgers, worktrees) — not repo source.
.superpowers/
.claude/

# Per-task working state (objective / progress / verification ledger) — not repo source.
.task/
